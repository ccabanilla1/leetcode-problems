#include <unordered_set>
#include <iostream>
#include <vector>

using namespace std;

class Solution {
  public:
      bool containsDuplicate(vector<int>& nums) {
          
          unordered_set<int> crossoff;
          for(int nums: x){
              crossoff.count(x){
                  return true;
              }
              crossoff.insert(x);
              return false;
          }
      }
  };
  
  int main(){
      containsDuplicate();
  }